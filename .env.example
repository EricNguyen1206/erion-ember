# =============================================================================
# Erion Ember - Environment Configuration
# =============================================================================
# Copy this file to .env and replace values with your own
# cp .env.example .env

# =============================================================================
# VECTOR INDEX BACKEND
# =============================================================================
# Options: 'annoy' (pure JS, default) or 'hnsw' (C++ optimized)
# Annoy.js: Works immediately, no build tools needed
# hnswlib: Requires C++ build tools (use Docker for easiest setup)
VECTOR_INDEX_BACKEND=annoy

# =============================================================================
# EMBEDDING SERVICE CONFIGURATION
# =============================================================================
# Provider: 'mock' (deterministic, for testing) or 'openai'
EMBEDDING_PROVIDER=mock

# OpenAI API Key (Required if EMBEDDING_PROVIDER=openai)
# Get your key at: https://platform.openai.com/api-keys
# OPENAI_API_KEY=sk-your-key-here

# =============================================================================
# CACHE CONFIGURATION
# =============================================================================
# Similarity threshold for semantic matching (default: 0.85)
# Higher = more strict matching, Lower = more permissive
CACHE_SIMILARITY_THRESHOLD=0.85

# Maximum cache entries (default: 100000)
CACHE_MAX_ELEMENTS=100000

# Default TTL for cache entries in seconds (default: 3600 = 1 hour)
CACHE_DEFAULT_TTL=3600

# =============================================================================
# SERVER CONFIGURATION
# =============================================================================
# Environment mode: development | production
NODE_ENV=development
